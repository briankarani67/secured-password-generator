# secured-password-generator
A simple and secure desktop password generator built with Python and Tkinter. Generates strong random passwords using letters, digits, and symbols, with a one-click copy-to-clipboard feature.## Secure Password Generator

A lightweight desktop application built with Python and Tkinter that generates strong, random passwords.  
It uses Python’s `secrets` module for cryptographically secure password generation.Always Use secured passwords

### Features
- Generates 16-character secure passwords
- Uses letters, numbers, and symbols
- One-click password copy to clipboard
- Simple and clean GUI

### Technologies Used
- Python 3
- Tkinter (GUI)
- secrets module
- string module

### How to Run
```bash
python passwordcreator.py

